import { Routes } from '@angular/router';
import { SplitPaymentComponent } from './split-payment/split-payment.component';

export const routes: Routes = [
  { path: '', component: SplitPaymentComponent }
];
